# How to use
pip install ipv64

# How to run
import ipv64

update_interval = 30 # seconds
domain = "XXX.vpn64.de"
update_key = "XXX"
discord_webhook = "XXX"

ipv64.ipv64(update_interval, domain, update_key, discord_webhook)